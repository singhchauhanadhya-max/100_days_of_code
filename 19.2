
#include<stdio.h>
void main()
{
    int num,remaining,sum=0;
    printf("Enter the number");
    scanf("%d",&num);
    while(num>0)
    {
        remaining=num%10;
        sum=sum+remaining;
        num=num/10;
    }
    printf("the sum of its digit is %d",sum);
}
