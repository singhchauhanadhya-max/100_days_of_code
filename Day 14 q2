#include<stdio.h>
int main()
{
    int a,n,sum=0;
    printf("enter the last number upto which user want");
    scanf("%d",&n);
    for(a=1;a<=n;a++)
    {
        if(a%2!=0)
        {
            sum=a+sum;
        }
    }
    printf("sum is %d",sum);
    return 0;
}
